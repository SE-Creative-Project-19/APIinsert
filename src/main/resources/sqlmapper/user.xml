<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="persistence.dto.UserDTO">

    <resultMap id="userResultMap" type="UserDTO">
        <id property="userPK" column="userPK" />
        <result property="ID" column="ID" />
        <result property="PW" column="PW" />
        <result property="phoneNumber" column="phoneNumber" />
        <result property="address" column="address" />
        <result property="mannerTemperature" column="mannerTemperature" />
        <result property="type" column="type" />
        <result property="name" column="name" />
        <result property="facility" column="facility" />
    </resultMap>

    <!-- 추가적인 쿼리들... -->

    <select id="getUser" resultMap="userResultMap">
        select ID, PhoneNumber, Address, MannerTemperature, Name
        from mydb.user as u
        where ID = #{ID};
    </select> <!--유저 정보 조회 -->

    <select id="getUsersByPk" resultMap="userResultMap">
        select ID, name, PhoneNumber, Address, MannerTemperature,
        from mydb.user as u
        where userPK = #{userPK};
    </select> <!--유저들 정보 조회  select * 하기에는 비번은 개인정보이고, 아이디는 나중에 업데이트 할 때 필요합니다.-->

    <select id="getId" resultType="String">
        select ID
        from mydb.user as u
        where phoneNumber = #{phoneNumber};
    </select> <!-- ID찾기 -->

    <update id="updateUserPassword">
        UPDATE mydb.user
        SET pw = #{pw}
        WHERE ID = #{id}
        AND phoneNumber = #{phoneNumber}
    </update> <!-- 비밀번호 재설정 -->
    <update id="updateUser">
        UPDATE mydb.user
        SET mannerTemperature = #{mannerTemperature}
        WHERE ID = #{id}
          AND phoneNumber = #{phoneNumber}
    </update> <!-- 비밀번호 재설정 -->

    <insert id="insertUser" parameterType="UserDTO">
        INSERT INTO user (ID, PW, PhoneNumber, Address, MannerTemperature, Type, Name)
        VALUES (#{ID}, #{PW}, #{PhoneNumber}, #{Address}, #{MannerTemperature}, #{Type}, #{Name})
    </insert> <!-- 회원가입 -->


    <select id="getUserByPK" resultType="String">
        select *
        from mydb.user
        where userPK = #{userPK};
    </select>
</mapper>
